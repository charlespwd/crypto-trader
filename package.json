{
  "name": "coincurry-monorepo",
  "version": "0.1.2",
  "private": true,
  "description": "",
  "author": "Charles-Philippe Clermont @charlespwd",
  "license": "MIT",
  "engine": {
    "node": ">=8.0.0"
  },
  "scripts": {
    "build": "lerna run build",
    "dev": "yarn dev:watch",
    "dev:cli": "cd packages/cli && yarn devel",
    "dev:watch": "lerna run dev --parallel --scope='!@coincurry/cli'",
    "precommit": "lerna run --parallel precommit",
    "start": "lerna run start",
    "test": "lerna run --parallel test",
    "test:watch": "lerna run --parallel test:watch"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "husky": "^0.14.3",
    "lerna": "^2.5.1"
  },
  "devDependencies": {
    "ts-node": "^3.1.0",
    "tslint": "^5.7.0",
    "tslint-config-airbnb": "^5.2.1",
    "typescript": "^2.5.2"
  }
}
